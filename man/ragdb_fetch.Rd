% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ragdb.R
\name{ragdb_fetch}
\alias{ragdb_fetch}
\title{Fetch the BiocAgentRAGDB from ExperimentHub}
\usage{
ragdb_fetch(force = FALSE)
}
\arguments{
\item{force}{Logical. If \code{TRUE}, re-downloads even if a local copy exists.
Default: \code{FALSE}.}
}
\value{
The file path to the local DuckDB database (invisibly).
}
\description{
Downloads (or retrieves from cache) the BiocAgentRAGDB DuckDB database
from ExperimentHub and stores it in a local \pkg{BiocFileCache}.
}
\details{
The database is hosted on Bioconductor ExperimentHub (resource ID: EHXXX).
On first call, it is downloaded from ExperimentHub and added to a local
\pkg{BiocFileCache}. On subsequent calls, the cached copy is returned
unless \code{force = TRUE}.

The BiocFileCache used is located at
\code{tools::R_user_dir("BiocAgentRAGDB", "cache")}.
}
\examples{
\dontrun{
# First-time download
db_path <- ragdb_fetch()

# Force re-download
db_path <- ragdb_fetch(force = TRUE)
}

}
