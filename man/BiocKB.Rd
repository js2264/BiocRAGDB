% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{BiocKB}
\alias{BiocKB}
\title{BiocKB S7 Class}
\usage{
BiocKB(
  db_path,
  embedding_model = "nomic-embed-text",
  overwrite = FALSE,
  read_only = FALSE
)
}
\arguments{
\item{db_path}{Character string. Path to the DuckDB file.}

\item{embedding_model}{Character string. Name of the Ollama embedding
model to use when creating a new store. Default: \code{"nomic-embed-text"}.}

\item{overwrite}{Logical. If \code{TRUE}, any existing store at \code{db_path} is
replaced when creating. Default: \code{FALSE}.}

\item{read_only}{Logical. If \code{TRUE}, connect to an existing store in
read-only mode. If \code{FALSE} (the default), create a new store.}
}
\value{
A \code{BiocKB} S7 object.
}
\description{
An S7 class representing a Bioconductor knowledge base backed by a
DuckDB ragnar store. The constructor can either create a new store or
connect to an existing one.
}
\examples{
\dontrun{
# Create a new store
kb <- BiocKB(tempfile(fileext = ".duckdb"))

# Connect to an existing store
kb <- BiocKB("biockb.duckdb", read_only = TRUE)
}

}
\seealso{
\code{\link[=insert_bioc_kb]{insert_bioc_kb()}}, \code{\link[=build_bioc_index]{build_bioc_index()}}, \code{\link[=retrieve_bioc_kb]{retrieve_bioc_kb()}}, \code{\link[=kb_path]{kb_path()}}
}
