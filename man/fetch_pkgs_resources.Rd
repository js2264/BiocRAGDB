% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{fetch_pkgs_resources}
\alias{fetch_pkgs_resources}
\title{Fetch Bioconductor Package Sources}
\usage{
fetch_pkgs_resources(
  base_dir,
  pkgs = NULL,
  branch = "devel",
  BPPARAM = BiocParallel::bpparam(),
  remove_clones = FALSE
)
}
\arguments{
\item{base_dir}{Character string. Path to the directory where packages
will be cloned and files will be collected. Created if it does not
exist.}

\item{pkgs}{Character vector of package names to fetch. When \code{NULL}
(the default), the full list of Bioconductor packages is obtained via
\code{biocPkgList()}.}

\item{branch}{Character string. Git branch to clone.
Default: \code{"devel"}.}

\item{BPPARAM}{A \code{\link[BiocParallel]{BiocParallelParam}} object
controlling parallelisation. Default: \code{BiocParallel::bpparam()}.}

\item{remove_clones}{Logical. If \code{TRUE}, the cloned repositories are
deleted after collecting files to save disk space.
Default: \code{FALSE}.}
}
\value{
A character vector of paths to the collected files (invisibly).
}
\description{
Clones Bioconductor packages from \url{https://git.bioconductor.org}
and collects their R source files, vignettes, \code{DESCRIPTION}, and
\code{NAMESPACE} into a single output directory for downstream processing.
}
\examples{
\dontrun{
files <- fetch_pkgs_resources(
    base_dir = tempdir(),
    pkgs = c("GenomicRanges", "IRanges"),
    branch = "devel"
)
head(files)
}

}
